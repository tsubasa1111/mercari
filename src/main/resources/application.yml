spring:
  datasource:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://localhost:5432/merucari
      username: postgres
      password: postgres
     

   
  batch:
   job:
    enabled: false